import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const ExpenseModal = (props: Props) => {
  return (
    <View>
      <Text>ExpenseModal</Text>
    </View>
  );
};

export default ExpenseModal;

const styles = StyleSheet.create({});
